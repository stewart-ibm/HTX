include ../../htx.mk

TARGET=hxecorsa

LDFLAGS +=

OBJ_SUFF=.o

VPATH= ../hxemem64/

OBJECTS = \
	hxemem${OBJ_SUFF} \
	mem_operation${OBJ_SUFF} \
	rand_operation${OBJ_SUFF} \
	pat_operation${OBJ_SUFF} \
	test_L4${OBJ_SUFF} \
	gen_tlbie${OBJ_SUFF} \
	corsa${OBJ_SUFF}

CFLAGS += -DCORSA_ENABLE -D__64_LINUX__ -D_GNU_SOURCE -DKERNEL_2_6 -D_REENTRANT -g -D__HTX_LINUX__ -D__USE_GNU -I./../hxemem64
 
LIBS += -lpthread -lhtx64 -lmisc64 -lhtxsyscfg64 -lzEDC

.PHONY: all clean

corsa.o: corsa.c
	${CC} ${CFLAGS} ${INCLUDES} -c $< -o $@

%.o: %.c
	${CC} ${CFLAGS} ${INCLUDES} -c $< -o $@

all: ${OBJECTS}
	${CC} ${LDFLAGS} ${LIBPATH} ${OBJECTS} ${LIBS} -o ${TARGET}
	${CP} ${TARGET} ${SHIPDIR}/${IDIR}


#%.o: %.s
#	${AS} ${CFLAGS} ${INCLUDES} -c $< -o $@

clean:
	${RM} -rf *.o ${TARGET} ${SHIPBIN}/${TARGET}

