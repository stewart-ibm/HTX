# Cleanup hxecpu previous run's seed list

echo "-------------------------------------------------------------------------------" >> ${HTX_LOG_DIR}/cpuLog

echo "Executing CPU runsetup on $(date)" >> ${HTX_LOG_DIR}/cpuLog
rm -rf ${HTX_LOG_DIR}/cpu*_seedlist.txt 2>/dev/null

